version: '3.9'

services:
  library-grpc-api:
    image: project/library-grpc-api
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "50051:50051"
    networks:
      - project





networks:
  project:
    name: project-stream
